spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop
    username: ssafy
    password: 8384
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
  database: sql_server

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
